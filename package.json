{
  "name": "graphbase-core",
  "version": "1.0.3",
  "description": "graphbase-core is library with generates backend resolvers based on GraphQL schema provided by user",
  "license": "MIT",
  "main": "app.js",
  "scripts": {
    "example": "npm run build -w graphbase-core && npm run start -w example",
    "example:windows": "npm run build:windows -w graphbase-core && npm run start -w example",
    "stucco": "npm run stucco -w example",
    "test": "npm run test -w graphbase-core "
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "graphbase-core": "file:packages/graphbase-core"
  }
}
