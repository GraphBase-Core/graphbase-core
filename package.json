{
  "name": "graphbase-native-client",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start:example": "npm run build -w graphbase-native && npm run start -w example"
  },
  "workspaces": [
    "packages/*"
  ]
}
